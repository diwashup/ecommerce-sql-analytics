# Data

Synthetic e-commerce data loaded into PostgreSQL/Supabase. Six relational tables are used: `customers`, `categories`, `products`, `orders`, `order_items`, and `payments`.

Raw CSV files are not included in this repository unless redistribution rights are confirmed. Load the same dataset into the tables before running the analysis scripts.
